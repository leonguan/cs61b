package check;

import board.Board;
import player.Move;


// Static class. Not meant to be instantiated, similar to the Math class.
public class Check {

	/**
	 * @param args
	 */
	// Checks whether a given move is valid
	public boolean isValid(Board board, Move move) {
		return true;
	}

}
